---
- name: Update apt package
  become: true
  apt:
    update_cache: yes

- name: Install Java 11
  become: true
  apt: 
    name: "{{java_package}}"
    state: present
  vars:
    java_package:
      - openjdk-11-jdk
